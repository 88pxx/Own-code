
from keras.models import load_model
from keras.utils import plot_model
model = load_model (r'Main/data/mdl.keras')
#model.save = (r'Main/data/1_model.h5')
#model.summary ()
#plot_model (model,
           # to_file = 'model.png',
            #show_shapes = True,
            #show_layers_names = True
            #)
